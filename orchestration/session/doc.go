// Package session orchestrates one agent step over runtime components.
//
// It composes core agent/environment/context models with runtime operation
// execution. It does not own persistence, protocol rendering, resource loading,
// daemon scheduling, or long-lived session stores yet.
package session
