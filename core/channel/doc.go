// Package channel defines pure channel envelope models.
//
// Channels normalize external protocol input into inbound envelopes and carry
// outbound messages/events back to adapters. Protocol parsing, rendering,
// delivery, streaming transport, and access enforcement live outside core.
package channel
